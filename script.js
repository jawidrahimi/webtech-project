function toggleMenu() {
    const menu = document.getElementById('dropdown-menu'); // Assuming you have a dropdown menu
    menu.classList.toggle('show'); // Toggles the 'show' class to display/hide the menu
}
